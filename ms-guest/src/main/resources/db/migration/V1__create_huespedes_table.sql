CREATE SCHEMA IF NOT EXISTS bd_huespedes;

CREATE TABLE IF NOT EXISTS bd_huespedes.huespedes (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    rut_pasaporte VARCHAR(50) NOT NULL UNIQUE,
    nombre VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL,
    telefono VARCHAR(50)
);